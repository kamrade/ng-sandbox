import { DynamicCasePipe } from './dynamic-case.pipe';

describe('DynamicCasePipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicCasePipe();
    expect(pipe).toBeTruthy();
  });
});
